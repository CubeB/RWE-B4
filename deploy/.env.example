# The hostname the certificate is issued for and the launcher connects to.
# An A record for it has to point at this machine before the first start, or
# Let's Encrypt cannot prove the name is yours.
RWE_MASTER_DOMAIN=master.example.com
