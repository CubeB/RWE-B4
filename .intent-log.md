# Intent log — sim-unit-state-field-table

- Derive the UnitState hash, save and dump walks from one declarative field table (the task; candidate 1 of the 2026-09-20 architecture review) [USER]
- addUnitFields were hand-woven; add a pinned test before touching the walks, compute the value from the pre-refactor code (469200823) — provenance: GameHash_util.test.cpp "a fully-populated unit keeps its pinned hash value" [AGENT]
- hash field order frozen, hash step must stay exactly `computeHashOf` over each member — provenance: the pinned-hash test [AGENT]
- rows ordered by the save walk, not by the hash walk; harmless because combineHashes folds by plain uint addition, so order never contributed to the value (precedents: GameHash_util.h `combineHashes`, GameHash_util.test.cpp "combineHashes"/"a fully-populated unit") [CODEBASE]
- the json plumbing primitives (float bit patterns, SimVector/SimAngle/GameTime/Energy/Metal, optional/enum, SaveContext/LoadContext, id-ref remapping) moved to SaveJson.h rather than being copied, because the table's steps in a second/third translation unit need them (precedents: save_util.cpp's "primitives" section it previously owned, sim_test_util.h's note on fourteen copies of makeEmptyCobScript) [CODEBASE]
- per-type save/load helpers hoisted out of save_util.cpp's anonymous namespace and declared in save_util.h, because the table is shared with GameHash_util and dump_util (precedents: save_util.h already exposes saveExploredGrid for its own test, save_util.cpp's bottom public functions) [CODEBASE]
- special substructures keep their specialised steps referenced by the row, still one row each: weapons (aims through the unit's own COB load-side threads), order queue (id remapping), behaviour/navigation/physics/factory variants (kinded encodings), pieces and cobEnvironment (save-only rows; the load reels them in at unit emplacement) — provenance: task's rule, CLAUDE.md's determinism section [USER]
- commandFireShotFired keeps its `j.value(..., false)` legacy read through an explicit `mayBeMissing` row flag, so saves predating the key still load [CODEBASE]
- never serialize state that was not saved before, never stop hashing what was hashed — verified field by field against the pre-refactor lists; the hash covers 60 rows, the save 65, the dump 46 (incl. armStowDueTime, which the hand-written dump had but only as its own row) [AGENT]
- the build/ scratch directory was never tracked on revival and is now explicitly ignored, so worktree build trees cannot leak into a branch [AGENT]
