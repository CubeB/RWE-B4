/libs/_msvc/
/libs/_protobuf-install/

/.cache/
/.idea/
# The gh CLI writes a device id here when HOME is not what it expects.
# Tool state and a machine identifier: not project content.
/.local/
/.vs/
crash.txt

# Screenshots taken while testing; they belong in the scratchpad, not the repo.
/*.png

# Build trees; the repo keeps nothing in them and CI rebuilds from scratch.
/build/
/build-gdebug/

# Sub-agent worktrees spawned during autonomous sessions.
/.claude/worktrees/

# The intent log is a session artifact; it is rendered into the PR
# description by the intent-stack skill, never checked in.
/.intent-log.md

# Captures of TA's network traffic hold LAN addresses and player names; tools/ta-net/README.md.
*.pcap
fakehost.log
fakehost-rx.jsonl
